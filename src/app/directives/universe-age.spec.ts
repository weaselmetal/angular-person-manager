import { UniverseAge } from './universe-age';

describe('UniverseAge', () => {
  it('should create an instance', () => {
    const directive = new UniverseAge();
    expect(directive).toBeTruthy();
  });
});
