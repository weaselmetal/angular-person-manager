import { NameAvailability } from './name-availability';

describe('NameAvailability', () => {
  it('should create an instance', () => {
    const directive = new NameAvailability();
    expect(directive).toBeTruthy();
  });
});
